// removing the declaration (specified in the header file)
// compiles and runs in C, despite the warnings
// while compilation fails in c++!
// #include "thing.h"

int main() {
  hello();
  return 0;
}
